{
  "name": "playwrightautomation",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "allureReportAllTest": "cross-env TEST_ENV=test && rimraf allure-results allure-report && npx playwright test tests/clientAppPO.spec.js && npx allure generate ./allure-results -o ./allure-report && npx allure open ./allure-report",
    "projectChrome": "rimraf allure-results allure-report && npx playwright test tests/clientAppPO.spec.js --config playwright.config1.js --project=chromeTest && npx allure generate ./allure-results -o ./allure-report && npx allure open ./allure-report",
    "UIPlaywright": "npx playwright test --ui --config playwright.config1.js --project=chromeTest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.3.3",
    "allure-commandline": "^2.29.0",
    "allure-playwright": "^3.6.0",
    "rimraf": "^6.1.3"
  },
  "dependencies": {
    "cross-env": "^10.1.0",
    "dotenv": "^17.4.0",
    "exceljs": "^4.4.0",
    "moment": "^2.30.1"
  }
}
